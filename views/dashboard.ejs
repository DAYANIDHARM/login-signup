<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Dashboard</title>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

<style>
/* Reset and Base Styles */
* {margin:0;padding:0;box-sizing:border-box;font-family:"Poppins",sans-serif;}

/* ⚠️ IMPORTANT: Vanta.js needs a simple, full-size container, so we remove all custom background code from body. */
body {
min-height:100vh;
display:flex;
justify-content:center;
align-items:center;
padding:20px;
color:#fff;
overflow-x:hidden;
/* Ensure the text is readable over the Vanta background */
z-index: 1; 
position: relative; 
}

/* Dashboard Box (The main card) */
.dashboard-box {
width:clamp(320px,90%,450px);
padding:30px; 
/* Keep the glassmorphism look */
background:rgba(255,255,255,0.1); 
box-shadow:0 10px 40px rgba(0,0,0,0.5); 
border-radius:25px; 
border:1px solid rgba(255,255,255,0.15); 
backdrop-filter:blur(20px); 
text-align:center;
transition:0.4s ease;
/* Ensure the card is on top of the Vanta canvas */
z-index: 10; 
position: relative;
}

/* Header Section */
.dashboard-header {
margin-bottom:30px;
}

.avatar {
width:80px;
height:80px;
background:rgba(255,255,255,0.4);
border-radius:50%;
margin:0 auto 15px;
display:flex;
justify-content:center;
align-items:center;
font-size:3rem;
color:#fff;
box-shadow:0 0 15px rgba(0,0,0,0.2);
}

.dashboard-box h2 {
margin-bottom:5px;
font-size:1.8rem;
font-weight:700;
text-shadow:0 2px 4px rgba(0,0,0,0.3);
}

.dashboard-box p {
font-size:0.9rem;
font-weight:300;
opacity:0.8;
}

/* Info Box */
.info-box {
margin:10px 0; 
padding:15px; 
background:rgba(255,255,255,0.15); 
border-radius:15px;
font-size:1.1rem;
display:flex;
align-items:center; 
justify-content:space-between;
transition:0.3s ease;
cursor:default;
}

.info-box:hover {
background:rgba(255,255,255,0.25);
transform:scale(1.02);
}

.info-box span:first-child {
font-weight:400; 
display:flex;
align-items:center;
gap:10px;
min-width:35%;
text-align:left;
}

.info-box span:last-child {
font-weight:600; 
color:#FFD700; 
text-align:right;
}

/* Logout Button */
.logout-btn {
width:100%;
padding:14px;
margin-top:35px;
background:linear-gradient(45deg,#FF0077,#FF00BB); 
border:none;
border-radius:15px;
font-size:1.2rem;
font-weight:600;
color:white;
cursor:pointer;
box-shadow:0 4px 15px rgba(255,0,100,0.4);
transition:0.3s;
}

.logout-btn:hover {
transform:scale(1.05);
box-shadow:0 6px 20px rgba(255,0,100,0.6);
}

/* Responsive Adjustments */
@media (max-width:450px) {
.dashboard-box { 
padding:25px 15px; 
border-radius:20px; 
}

.dashboard-header {
margin-bottom:20px;
}

.dashboard-box h2 {font-size:1.6rem;}
.dashboard-box p {font-size:0.85rem;}

.info-box { 
padding:10px 12px; 
font-size:0.95rem; 
flex-direction:column; 
gap:5px; 
text-align:center;
}

.info-box span:first-child {
min-width:auto; 
justify-content:center;
}

.info-box span:last-child {
color:#FFD700; 
font-weight:700;
}

.logout-btn { 
padding:10px; 
font-size:1.1rem; 
margin-top:20px;
}
}

@media (max-height:500px) and (orientation:landscape) {
body {padding:10px;}
.dashboard-box { 
width:clamp(320px,70%,450px);
padding:15px; 
}
.dashboard-box h2 {font-size:1.4rem;margin-bottom:5px;}
.avatar {width:60px;height:60px;font-size:2rem;}
.info-box { 
font-size:0.9rem; 
padding:6px 8px;
margin:6px 0;
flex-direction:row;
justify-content:space-between;
text-align:left;
}
.logout-btn {padding:8px;font-size:1rem;margin-top:10px;}
}
</style>
</head>

<body>

<div class="dashboard-box">

    <div class="dashboard-header">
        <div class="avatar">
            <i class="fa-solid fa-user"></i>
        </div>
        <h2>Welcome, User</h2>
        <p>This is your personalized control panel.</p>
    </div>

    <div class="info-box">
        <span><i class="fa-solid fa-signature"></i> Username</span>
        <span><%= user.username %></span>
    </div>

    <div class="info-box">
        <span><i class="fa-solid fa-envelope"></i> Email</span>
        <span><%= user.email %></span>
    </div>

    <div class="info-box">
        <span><i class="fa-solid fa-lock"></i> Password</span>
        <span>********</span>
    </div>

    <form action="/logout">
        <button type="submit" class="logout-btn">Logout</button>
    </form>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r121/three.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.net.min.js"></script>

<script>
VANTA.NET({
  el: "body", // Target the body element
  mouseControls: true,
  touchControls: true,
  minHeight: 200.00,
  minWidth: 200.00,
  scale: 1.00,
  scaleMobile: 1.00,
  color: 0xbb00ff, // Primary color (Purple/Pink)
  backgroundColor: 0x1c007e, // Darker base color
  points: 10.00, // Number of points
  spacing: 20.00, // Spacing between points
  showDots: true
})
</script>

</body>
</html>